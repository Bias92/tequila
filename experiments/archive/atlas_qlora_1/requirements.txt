# Pinned to versions known to work together with PyTorch 2.4 / CUDA 12.1.
# RunPod templates "PyTorch 2.4" / "PyTorch 2.5" already ship torch + cu12.

torch>=2.4.0
transformers>=4.46.0,<4.50.0
accelerate>=1.0.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0
datasets>=3.0.0
sentencepiece
protobuf

# Eval / format
rouge-score
bert-score
scikit-learn
numpy

# Optional
wandb
tensorboard
